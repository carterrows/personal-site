services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-site
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 4321
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    ports:
      - "4321:4321"
